//Fractional Knapsack – Relief Material Loading
#include<stdio.h>
int main()
{
    int weights[]={4,8,2,6};
    int profits[]={20,40,14,30};
    int n=4,capacity=10,i,j,tempW,tempP;
    float ratio[4],tempR,totalProfit=0;
    for(i=0;i<n;i++)
        ratio[i]=(float)profits[i]/weights[i];
    for(i=0;i<n-1;i++)
    {
        for(j=i+1;j<n;j++)
        {
            if(ratio[i]<ratio[j])
            {
                tempR=ratio[i];
                ratio[i]=ratio[j];
                ratio[j]=tempR;
                tempW=weights[i];
                weights[i]=weights[j];
                weights[j]=tempW;
                tempP=profits[i];
                profits[i]=profits[j];
                profits[j]=tempP;
            }
        }
    }
    for(i=0;i<n;i++)
    {
        if(capacity>=weights[i])
        {
            capacity-=weights[i];
            totalProfit+=profits[i];
        }
        else
        {
            totalProfit+=ratio[i]*capacity;
            capacity=0;
            break;
        }
    }
    printf("Maximum profit = %.0f\n",totalProfit);
    return 0;
}
