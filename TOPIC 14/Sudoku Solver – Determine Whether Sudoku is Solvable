def is_safe(grid, row, col, num):
    # Check row
    for j in range(9):
        if grid[row][j] == num:
            return False
    
    # Check column
    for i in range(9):
        if grid[i][col] == num:
            return False
    
    # Check 3x3 box
    start_row = (row // 3) * 3
    start_col = (col // 3) * 3
    for i in range(start_row, start_row + 3):
        for j in range(start_col, start_col + 3):
            if grid[i][j] == num:
                return False
    
    return True

def is_solvable(grid):
    # Make a copy to avoid modifying original
    grid_copy = [row[:] for row in grid]
    
    def solve(grid):
        for i in range(9):
            for j in range(9):
                if grid[i][j] == 0:
                    for num in range(1, 10):
                        if is_safe(grid, i, j, num):
                            grid[i][j] = num
                            if solve(grid):
                                return True
                            grid[i][j] = 0
                    return False
        return True
    
    return solve(grid_copy)

def print_grid(grid):
    for row in grid:
        print(' '.join(map(str, row)))

# Test case 1: Solvable puzzle
grid1 = [
    [5, 3, 0, 0, 7, 0, 0, 0, 0],
    [6, 0, 0, 1, 9, 5, 0, 0, 0],
    [0, 9, 8, 0, 0, 0, 0, 6, 0],
    [8, 0, 0, 0, 6, 0, 0, 0, 3],
    [4, 0, 0, 8, 0, 3, 0, 0, 1],
    [7, 0, 0, 0, 2, 0, 0, 0, 6],
    [0, 6, 0, 0, 0, 0, 2, 8, 0],
    [0, 0, 0, 4, 1, 9, 0, 0, 5],
    [0, 0, 0, 0, 8, 0, 0, 7, 9]
]

# Test case 2: Unsolvable puzzle (duplicate in row)
grid2 = [
    [5, 3, 0, 0, 7, 0, 0, 0, 5],  # Duplicate 5 in row
    [6, 0, 0, 1, 9, 5, 0, 0, 0],
    [0, 9, 8, 0, 0, 0, 0, 6, 0],
    [8, 0, 0, 0, 6, 0, 0, 0, 3],
    [4, 0, 0, 8, 0, 3, 0, 0, 1],
    [7, 0, 0, 0, 2, 0, 0, 0, 6],
    [0, 6, 0, 0, 0, 0, 2, 8, 0],
    [0, 0, 0, 4, 1, 9, 0, 0, 5],
    [0, 0, 0, 0, 8, 0, 0, 7, 9]
]

# Test solvable puzzle
print("Testing Solvable Puzzle:")
if is_solvable(grid1):
    print("Sudoku Solvable: YES")
    print("Solution Found Successfully")
else:
    print("Sudoku Solvable: NO")

print("\n" + "-"*40 + "\n")

# Test unsolvable puzzle
print("Testing Unsolvable Puzzle:")
if is_solvable(grid2):
    print("Sudoku Solvable: YES")
    print("Solution Found Successfully")
else:
    print("Sudoku Solvable: NO")
    print("No valid solution exists for this puzzle")
